namespace Core.Web.Schemas
{
    /// <summary>
    /// These correspond to the type of pages that the client reads the data for.
    /// </summary>
    public enum ColonyPages
    {
        Overview,
        Buildings,
        Resources
    }
}
