namespace Core.Web.Schemas.Port
{
    //public class ShipPortGood : PortGood
    //{
    //    [JsonConverter(typeof(StringEnumConverter))]
    //    public ShipTypes ShipType { get; protected set; }

    //    /// <summary>
    //    /// TODO: consider making a UI ship stats which is just a subset of all the ship stats to avoid exposing too much to the client
    //    /// </summary>
    //    public ShipStats ShipStats { get; protected set; }



    //    public ShipPortGood(PortGoodIdentifier goodId, string assetUrl, float purchasePrice, float sellPrice, ShipStats shipStats, ShipTypes shipType)
    //        : base(goodId, assetUrl, shipStats.Description, shipStats.Name, purchasePrice, sellPrice, PortGoodCategory.Ship)
    //    {
    //        ShipType = shipType;
    //        ShipStats = shipStats;
    //    }
    //}
}